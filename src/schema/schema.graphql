type Query {
  hello: String
  say(name: String!): String
}

type Account {
  id: ID!
  name: String
  cpf: String
  createdAt: String
}

type Mutation {
  accountCreate(name: String, cpf: String): Account
}